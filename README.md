# Python-Normalizing-Currency-Format
Normalizing and standardizing inconsistent currency formats into structured numeric values. Handles symbols, separators, and locale differences using regex and pandas. Concepts: data cleaning, parsing, string processing, ETL transformation, and currency normalization.
